# Copia a .env.local y rellena con los valores reales.
# .env.local esta gitignored por defecto en Vite.

# URL del Worker de Cloudflare (envios de emails transaccionales tras checkout)
# - En local (wrangler dev):    http://127.0.0.1:8787
# - En produccion:              https://worker.groweldruida.es (o tu URL de workers.dev)
# Deja vacio si todavia no tienes Worker desplegado — el checkout sigue funcionando
# con WhatsApp/email manual igual que antes.
VITE_WORKER_URL=
